{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.taskView'|trans({'%id%': task.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1> {{ 'title.taskView'|trans({'%id%': task.id|default('')}) }} </h1>

    {% if task is defined and task|length %}

        <dl class="dl-horizontal">
            <dt> ID </dt>
            <dd> {{ task.id }} </dd>
            <dt> Created at </dt>
            <dd> {{ task.createdAt|date('Y-m-d') }} </dd>
            <dt> Updated at </dt>
            <dd> {{ task.updatedAt|date('Y-m-d') }} </dd>
            <dt> Category </dt>
            <dd> {{ task.category.title }} </dd>
            <dt> Title </dt>
            <dd> {{ task.title }} </dd>
        </dl>
        <p>
            <a href="{{ url('task_index') }}" title="Back to list">
                Back to list
            </a>
        </p>

    {% else %}

        <p>
            Record not found!
        </p>

    {% endif %}

{% endblock %}